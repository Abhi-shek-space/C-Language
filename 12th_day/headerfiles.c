// #include "Tute_12.c"//including local directories of system

#include  <stdio.h>
#define pi 3.14 //replace value form 1 to 2 every pi in program is replaced by 3.14 before compilation in preprocessing
#define square(x) x*x//macros which faster that functions and works as functions.
  #if def //if define a macro some to print about bts of program 
  #if undef //if undefine a macro then bts of program using printf command
#if
#elif
#else


#undef //undine the define fuction
int main()

{
    float v= pi;
    int a=3;
    printf("%f\n",pi);
    printf("The area of circle is %f\n",pi*square(a));
    
    return 0;
}